apiVersion: v1
kind: Service
metadata:
  name: {{ include "accesspoint-in-endpoint.fullname" . }}
  labels:
    {{- include "accesspoint-in-endpoint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "accesspoint-in-endpoint.selectorLabels" . | nindent 4 }}
