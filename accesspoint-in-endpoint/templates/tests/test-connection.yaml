apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accesspoint-in-endpoint.fullname" . }}-test-connection"
  labels:
{{ include "accesspoint-in-endpoint.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "accesspoint-in-endpoint.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
